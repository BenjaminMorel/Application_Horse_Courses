<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/background</item>
        <item name="colorPrimaryDark">@color/button2</item>
        <item name="colorAccent">@color/text_edit</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AlertDialogCustom" parent="Theme.AppCompat.Dialog">
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:colorAccent">@color/black</item>
    </style>

    <style name="CalendarCustom" parent="Theme.AppCompat.Dialog">
        <item name="colorAccent">@color/text_edit</item>
        <item name="colorPrimaryDark">@color/textColor</item>
        <item name="colorPrimary">@color/textColor</item>
        <item name="android:weekNumberColor">@color/textColor</item>
        <item name="android:headerYearTextAppearance">@color/textColor</item>
        <item name="android:colorAccent">#858585</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textColorPrimary">@color/textColor</item>
    </style>

</resources>